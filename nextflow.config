profiles {
  standard {
    process {
      withName:'.*' {
        container = 'phage_annotation.sif'
        errorStrategy = 'retry'
        maxRetries = 2
      }
    }
    singularity {
    enabled = true
    runOptions = '-B /net/storage:/net/storage'
    autoMounts = true
    } 
  } 
}
